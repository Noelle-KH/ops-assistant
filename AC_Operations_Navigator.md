# Acceptance Criteria｜運營領航站（Operations Navigator）
**文件版本：** v1.0  
**建立日期：** 2026-04-30  
**對應 PRD：** PRD_Operations_Navigator v1.0  
**文件狀態：** 待審閱

---

## 文件說明

本文件以 **Given / When / Then** 格式定義各功能模塊的驗收標準。每條 AC 對應一個可驗證的使用者行為或系統行為。

標記說明：
- 🔴 **P0** — 第一版上線必要，不通過不可上線
- 🟡 **P1** — 第一版應完成，有合理替代方案可協商
- 🟢 **P2** — 第一版預留，後續版本完成

---

## AC-01 全域導覽與介面框架

### AC-01-1 左側固定側欄（🔴 P0）
- **Given** 使用者已登入系統
- **When** 在任何頁面上
- **Then** 左側側欄持續顯示，包含：首頁、FAQ、SOP、郵件模板、群組目錄、系統工具、歸屬鏈解析器等導覽連結，點擊任一連結可正確切換對應頁面

### AC-01-2 首頁儀表板顯示（🔴 P0）
- **Given** 使用者已登入系統
- **When** 進入首頁
- **Then** 顯示以下區塊：
  - 各模塊快捷入口卡片
  - 歸屬鏈解析器快捷輸入區
  - 產品公告預留區塊（第一版顯示「即將推出」佔位說明）

### AC-01-3 全域搜尋（🟡 P1）
- **Given** 使用者在任何頁面
- **When** 在側欄搜尋列輸入關鍵字並按下 Enter 或點擊搜尋
- **Then** 顯示跨模塊搜尋結果，包含 FAQ、SOP、郵件模板中符合關鍵字的條目，結果顯示所屬模塊類型與標題，點擊可跳轉至對應頁面

### AC-01-4 當前頁面狀態標示（🔴 P0）
- **Given** 使用者在任何模塊頁面
- **When** 查看側欄
- **Then** 當前所在模塊的導覽項目有視覺高亮標示，與其他項目有明顯區別

---

## AC-02 RWD 響應式設計

### AC-02-1 桌機版面（🔴 P0）
- **Given** 瀏覽器寬度 ≥ 1280px
- **When** 使用者進入任何頁面
- **Then** 左側固定側欄展開顯示，內容區域正常渲染，無水平捲軸

### AC-02-2 平板版面（🔴 P0）
- **Given** 瀏覽器寬度介於 768px – 1279px
- **When** 使用者進入任何頁面
- **Then** 側欄預設展開，使用者可點擊收合，收合後主內容區域自動擴展填滿

### AC-02-3 手機版面（🔴 P0）
- **Given** 瀏覽器寬度 < 768px
- **When** 使用者進入任何頁面
- **Then** 側欄隱藏，改以底部 Tab 導覽列替代，所有主要模塊均可透過底部導覽存取，內容區域單欄顯示無遮擋

---

## AC-03 FAQ 知識庫

### AC-03-1 標籤分類篩選（🔴 P0）
- **Given** 使用者進入 FAQ 頁面
- **When** 點擊任一分類標籤（如「開戶」「入金」「出金」等）
- **Then** 列表僅顯示屬於該分類的 FAQ 條目，其他分類條目隱藏；再次點擊同一標籤則回復顯示全部

### AC-03-2 關鍵字搜尋（🔴 P0）
- **Given** 使用者在 FAQ 頁面
- **When** 在搜尋列輸入關鍵字
- **Then** 即時篩選顯示問題標題或內容中包含該關鍵字的條目，無符合結果時顯示「查無符合結果，請嘗試其他關鍵字」

### AC-03-3 標準回覆顯示（🔴 P0）
- **Given** 使用者在 FAQ 列表中
- **When** 點擊任一 FAQ 條目
- **Then** 展開顯示標準回覆內容，再次點擊則收合

### AC-03-4 運營備注展開（🔴 P0）
- **Given** 使用者已展開一條 FAQ 條目，且該條目有運營備注資料
- **When** 點擊「運營備注」展開按鈕
- **Then** 顯示運營備注內容；若無備注資料，則不顯示該展開按鈕

### AC-03-5 英文回覆翻譯按鈕（🟡 P1）
- **Given** 使用者已展開一條有英文回覆的 FAQ 條目
- **When** 點擊「英文回覆」按鈕
- **Then** 顯示該條目的英文版回覆內容；再次點擊則收合；若無英文資料，則不顯示此按鈕

### AC-03-6 關聯 SOP 跳轉（🟡 P1）
- **Given** 一條 FAQ 條目有關聯 SOP
- **When** 使用者在展開的 FAQ 中點擊「查看相關 SOP」連結
- **Then** 導向至對應 SOP 頁面，並自動捲動至該 SOP 內容

### AC-03-7 關聯模板跳轉（🟡 P1）
- **Given** 一條 FAQ 條目有關聯郵件模板
- **When** 使用者在展開的 FAQ 中點擊「使用對應模板」連結
- **Then** 導向至郵件模板庫，並自動開啟對應模板

---

## AC-04 SOP 操作流程

### AC-04-1 SOP 列表顯示（🔴 P0）
- **Given** 使用者進入 SOP 頁面
- **When** 頁面載入
- **Then** 顯示所有已發布的 SOP，依類別分組呈現，每個項目顯示標題與類別標籤

### AC-04-2 三層結構標籤切換（🔴 P0）
- **Given** 使用者已選取一份 SOP
- **When** 點擊「規則」、「OA 操作步驟」或「例外情況」標籤
- **Then** 內容區域切換至對應區段內容，當前標籤有視覺高亮標示

### AC-04-3 規則區段顯示（🔴 P0）
- **Given** 使用者查看 SOP 的「規則」標籤
- **When** 頁面顯示
- **Then** 清楚呈現規則說明、適用條件與限制，條列式排版，重要條件有視覺強調

### AC-04-4 OA 操作步驟顯示（🔴 P0）
- **Given** 使用者查看 SOP 的「OA 操作步驟」標籤
- **When** 頁面顯示
- **Then** 步驟以編號逐條顯示，OA 系統路徑（如「OA > 資料管理 > 用戶管理」）以特殊樣式高亮標示，與一般說明文字有視覺區別

### AC-04-5 例外情況顯示（🔴 P0）
- **Given** 使用者查看 SOP 的「例外情況」標籤
- **When** 頁面顯示
- **Then** 每個例外情境清楚標示情境描述與對應處理方式，若無例外情況則顯示「本 SOP 無已知例外情況」

### AC-04-6 第一版必要 SOP 存在（🔴 P0）
- **Given** 系統第一版上線
- **When** 使用者進入 SOP 頁面
- **Then** 以下三份 SOP 已建立且完整可閱讀：
  1. 轉移歸屬
  2. 負餘額保護審核
  3. 取款審核判斷

---

## AC-05 郵件模板庫

### AC-05-1 標籤分類篩選（🔴 P0）
- **Given** 使用者進入郵件模板頁面
- **When** 點擊任一分類標籤
- **Then** 僅顯示該類別的模板，其他隱藏

### AC-05-2 卡片選擇模板版本（🔴 P0）
- **Given** 一個模板情境有多個版本
- **When** 頁面顯示
- **Then** 各版本以卡片呈現，每張卡片顯示版本名稱（如「進度型安撫」）與適用情境描述；不預設展開任何版本內容

### AC-05-3 卡片展開模板內容（🔴 P0）
- **Given** 使用者看到模板版本卡片列表
- **When** 點擊任一版本卡片
- **Then** 展開顯示完整模板內容；同時最多只有一個版本展開，點擊其他版本卡片時自動收合前一個

### AC-05-4 無欄位模板一鍵複製（🔴 P0）
- **Given** 使用者已展開一個不含填寫欄位的模板版本
- **When** 點擊「複製」按鈕
- **Then** 模板全文複製至剪貼簿，按鈕暫時顯示「已複製！」確認訊息（持續約 2 秒後恢復）

### AC-05-5 含欄位模板填寫後複製（🔴 P0）
- **Given** 使用者已展開一個含 `{{欄位}}` 佔位符的模板版本
- **When** 頁面顯示
- **Then** 欄位區域顯示可填寫的輸入框，附帶欄位名稱標籤與範例提示；使用者填寫欄位後，預覽區域即時更新為填入值後的完整文字；點擊「複製」後複製最終完整文字至剪貼簿

### AC-05-6 欄位未填寫時的警示（🟡 P1）
- **Given** 使用者展開含欄位模板，有欄位未填寫
- **When** 點擊「複製」按鈕
- **Then** 顯示警示訊息「仍有欄位尚未填寫，確定要複製嗎？」，提供「確認複製」與「返回填寫」選項；選擇確認複製則複製含 `{{欄位}}` 未替換的原始文字

### AC-05-7 關聯跳轉（🟡 P1）
- **Given** 一個模板有關聯 FAQ 或 SOP
- **When** 使用者在模板頁面
- **Then** 顯示「查看相關 FAQ」或「查看相關 SOP」連結，點擊後跳轉至對應頁面

---

## AC-06 群組與聯絡人目錄

### AC-06-1 部門分區顯示（🔴 P0）
- **Given** 使用者進入群組目錄頁面
- **When** 頁面載入
- **Then** 群組依部門分區顯示（運營／金流／產品／機器人），每個群組顯示名稱、用途說明、負責人標籤

### AC-06-2 用途說明完整性（🔴 P0）
- **Given** 系統第一版上線
- **When** 使用者查看群組目錄
- **Then** 所有現有群組均已建立，每個群組至少包含：名稱、用途描述、所屬部門

---

## AC-07 系統工具與帳號資源

### AC-07-1 連結清單顯示（🔴 P0）
- **Given** 使用者進入系統工具頁面
- **When** 頁面載入
- **Then** 顯示系統連結清單，每個連結顯示系統名稱、備注說明，點擊後在新分頁開啟對應網址

### AC-07-2 帳號資源存取控制（🔴 P0）
- **Given** 一般運營人員（非高級）已登入
- **When** 查看系統工具頁面
- **Then** 敏感帳號資訊區塊不可見，或顯示「您的帳號無權限查看此內容」

### AC-07-3 高級權限帳號查看敏感資訊（🔴 P0）
- **Given** 具有高級運營或管理者權限的使用者已登入
- **When** 在帳號資源區點擊「顯示」按鈕
- **Then** 需二次確認（如點擊確認彈窗），確認後顯示帳號明文資訊

### AC-07-4 帳密自動隱藏（🟡 P1）
- **Given** 使用者已展開顯示帳密資訊
- **When** 離開該頁面後再返回，或超過 5 分鐘無操作
- **Then** 帳密資訊自動收合隱藏，需重新點擊「顯示」才能再次查看

---

## AC-08 歸屬鏈解析器

### AC-08-1 正常解析輸出（🔴 P0）
- **Given** 使用者在歸屬鏈解析器頁面（或首頁快捷區）
- **When** 在輸入框貼入格式正確的歸屬鏈字串，並點擊「解析」按鈕
- **Then** 輸出區域顯示正確轉換結果，格式為 `{姓名}({顯示標示}) ,{姓名}({顯示標示}) ,...`，ID 不顯示

### AC-08-2 角色代碼對應正確性（🔴 P0）
- **Given** 輸入包含各種角色代碼的歸屬鏈
- **When** 執行解析
- **Then** 各角色代碼對應顯示如下，且無例外：
  - `sales` → `业务员`
  - `Level I` → `Level I`
  - `Level II` → `Level II`
  - `Level Ⅲ` → `Level Ⅲ`
  - `user` → `直客`

### AC-08-3 輸出順序與輸入一致（🔴 P0）
- **Given** 使用者輸入有順序的歸屬鏈字串
- **When** 執行解析
- **Then** 輸出結果的排列順序與輸入順序完全一致，不重新排序

### AC-08-4 一鍵複製解析結果（🔴 P0）
- **Given** 解析器已成功產生輸出結果
- **When** 使用者點擊「複製」按鈕
- **Then** 輸出文字複製至剪貼簿，按鈕顯示「已複製！」確認訊息約 2 秒

### AC-08-5 輸入為空時的錯誤提示（🔴 P0）
- **Given** 使用者未在輸入框填入任何內容
- **When** 點擊「解析」按鈕
- **Then** 輸入框下方顯示紅色錯誤訊息：「請輸入歸屬鏈資料後再進行解析」，不執行解析

### AC-08-6 欄位數量錯誤的錯誤提示（🔴 P0）
- **Given** 使用者輸入的字串中，逗號分隔後的欄位總數不為 3 的倍數
- **When** 點擊「解析」按鈕
- **Then** 顯示錯誤訊息：「格式錯誤：資料欄位數量不符，每筆資料應包含 ID、角色、姓名三個欄位」，不產生輸出

### AC-08-7 無法識別角色代碼的錯誤提示（🔴 P0）
- **Given** 使用者輸入的字串包含系統無法識別的角色代碼
- **When** 點擊「解析」按鈕
- **Then** 顯示錯誤訊息：「第 X 筆資料的角色代碼「{代碼}」無法識別，請確認資料格式」，X 為該筆資料的位置順序，不產生輸出

### AC-08-8 姓名欄位為空的錯誤提示（🔴 P0）
- **Given** 使用者輸入的字串中，某筆資料的姓名欄位為空字串
- **When** 點擊「解析」按鈕
- **Then** 顯示錯誤訊息：「第 X 筆資料的姓名欄位為空，請確認資料完整性」，不產生輸出

### AC-08-9 首頁快捷入口可用（🔴 P0）
- **Given** 使用者在首頁儀表板
- **When** 在歸屬鏈解析器快捷區輸入資料並解析
- **Then** 功能行為與獨立頁面完全一致，包含錯誤提示與複製功能

---

## AC-09 管理者後台

### AC-09-1 獨立登入入口（🔴 P0）
- **Given** 任何使用者
- **When** 嘗試透過一般使用者登入進入管理者後台
- **Then** 無法存取，管理者後台有獨立的登入頁面與認證機制

### AC-09-2 FAQ 新增（🔴 P0）
- **Given** 管理者已登入後台
- **When** 填寫新 FAQ 的所有必填欄位（分類、標籤、問題、標準回覆）並點擊「儲存」
- **Then** 新 FAQ 出現在使用者端 FAQ 列表中，顯示正確內容

### AC-09-3 FAQ 編輯（🔴 P0）
- **Given** 管理者已登入後台
- **When** 選取現有 FAQ，修改內容後點擊「儲存」
- **Then** 使用者端 FAQ 更新為修改後的內容，變更記錄於稽核日誌中

### AC-09-4 FAQ 停用（🔴 P0）
- **Given** 管理者已登入後台
- **When** 將某 FAQ 標記為「停用」
- **Then** 該 FAQ 從使用者端列表消失，但在管理者後台仍可查看（以「已停用」標籤標示），資料不被刪除

### AC-09-5 郵件模板管理（🔴 P0）
- **Given** 管理者已登入後台
- **When** 執行模板的新增、編輯、停用操作
- **Then** 行為與 AC-09-2 至 AC-09-4 的 FAQ 管理相同，包含所有欄位與多版本的視覺化編輯

### AC-09-6 SOP 管理（🔴 P0）
- **Given** 管理者已登入後台
- **When** 執行 SOP 的新增、編輯、停用操作
- **Then** 三層結構（規則/OA操作/例外情況）均可獨立編輯，行為與 AC-09-2 至 AC-09-4 相同

### AC-09-7 帳號建立（🔴 P0）
- **Given** 管理者已登入後台
- **When** 建立新使用者帳號，設定帳號名稱、密碼與存取層級（一般運營 / 高級運營 / 管理者）
- **Then** 新帳號可使用設定的憑證登入，並依存取層級看到對應的內容與功能

### AC-09-8 帳號停用（🔴 P0）
- **Given** 管理者已登入後台
- **When** 停用某使用者帳號
- **Then** 該帳號立即無法登入，已登入的 Session 於下次請求時失效

### AC-09-9 稽核記錄（🟡 P1）
- **Given** 管理者在後台執行任何內容新增、編輯、停用操作
- **When** 操作完成
- **Then** 稽核日誌記錄：操作者帳號、操作類型、操作對象、操作時間，可在後台查詢

---

## AC-10 效能與安全

### AC-10-1 頁面載入時間（🟡 P1）
- **Given** 使用者在正常網路環境（桌機）
- **When** 切換至任何模塊頁面
- **Then** 頁面主要內容在 3 秒內完成渲染

### AC-10-2 搜尋回應時間（🟡 P1）
- **Given** 使用者在 FAQ 頁面輸入搜尋關鍵字
- **When** 輸入完成後
- **Then** 篩選結果在 0.5 秒內更新顯示

### AC-10-3 Session 逾時（🔴 P0）
- **Given** 已登入的使用者
- **When** 超過 60 分鐘無任何操作
- **Then** Session 自動失效，下次操作時導向至登入頁面，需重新登入

### AC-10-4 未登入者無法存取（🔴 P0）
- **Given** 未登入的使用者
- **When** 直接輸入系統任何頁面的 URL
- **Then** 自動重導向至登入頁面，無法存取任何系統內容

---

## 附錄：驗收清單摘要

| 模塊 | AC 編號 | 優先級 | 說明 |
|------|---------|--------|------|
| 導覽框架 | AC-01-1 ~ 01-4 | P0/P1 | 側欄、儀表板、搜尋、狀態標示 |
| RWD | AC-02-1 ~ 02-3 | P0 | 桌機、平板、手機三個斷點 |
| FAQ | AC-03-1 ~ 03-7 | P0/P1 | 篩選、搜尋、展開、翻譯、跳轉 |
| SOP | AC-04-1 ~ 04-6 | P0 | 列表、三層結構、三份優先 SOP |
| 郵件模板 | AC-05-1 ~ 05-7 | P0/P1 | 篩選、卡片、複製、欄位填寫 |
| 群組目錄 | AC-06-1 ~ 06-2 | P0 | 分區顯示、資料完整性 |
| 系統工具 | AC-07-1 ~ 07-4 | P0/P1 | 連結、存取控制、自動隱藏 |
| 歸屬鏈解析器 | AC-08-1 ~ 08-9 | P0 | 解析邏輯、錯誤處理、複製、首頁入口 |
| 管理者後台 | AC-09-1 ~ 09-9 | P0/P1 | 登入、CRUD、帳號管理、稽核 |
| 效能安全 | AC-10-1 ~ 10-4 | P0/P1 | 載入時間、搜尋回應、Session、存取控制 |

---

## 版本記錄

| 版本 | 日期 | 說明 |
|------|------|------|
| v1.0 | 2026-04-30 | 初版建立 |
